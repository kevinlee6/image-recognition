<h1>Tags</h1>

<div class="ui three stackable cards">
  <% @tags.each do |tag| %>
    <%= link_to tag.tag.upcase, tag_path(tag.tag), class: 'ui primary button' %>
  <% end %>
</div>
<br>

<%= link_to 'New Tag', new_tag_path %>
